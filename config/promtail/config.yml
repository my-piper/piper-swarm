server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: https://logs.pornworks.ai/loki/api/v1/push
    basic_auth:
      username: worker
      password: jGuH2T2quoFC4RV8

scrape_configs:
  - job_name: backend
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 15s
        filters:
          - name: label
            values: ["com.docker.swarm.service.name=piper_backend"]
    pipeline_stages:
      - json:
          expressions:
            level:
            loggerName:
      - labels:
          level:
            #loggerName:
      - static_labels:
          env: ${ENV:-dev}
          component: piper-xempire-backend
  - job_name: piper_worker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 15s
        filters:
          - name: label
            values: ["com.docker.swarm.service.name=piper_worker"]
    pipeline_stages:
      - json:
          expressions:
            level:
            loggerName:
      - labels:
          level:
            #loggerName:
      - static_labels:
          env: ${ENV:-dev}
          component: piper-xempire-worker

